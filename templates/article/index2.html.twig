{% extends 'base.html.twig' %}

{% block body %}
   {% for article in articles %}
      <h2> {{article.titre}} </h2>
      <div> {{article.date | date('d-m-Y H:i:s')}} </div>
      <div> {{article.user.nom | upper}} {{article.user.prenom}} </div>
   {% endfor %} 
{% endblock %}